'use client';

import React from 'react';

const Dashboard: React.FC = () => {
  return (
    <div id="dashboard" className="mt-20">
      <h2 className="text-3xl font-bold">Dashboard</h2>
      <p>Here you can add the content for the section...</p>
    </div>
  );
};

export default Dashboard;
